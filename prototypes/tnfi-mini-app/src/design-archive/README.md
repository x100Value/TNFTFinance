# Design Archive

- `App.eggshell.snapshot.tsx` and `App.eggshell.snapshot.css` keep the cleaned dual-theme layout snapshot.
- Open eggshell variant with `?theme=eggshell`.
- Open graphite variant with `?theme=graphite`.
